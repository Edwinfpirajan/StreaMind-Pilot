def interpret_command(text):
    # Simulamos que IA interpreta y responde acción
    print(f"Interpretando intención para: '{text}'")
    return "save_replay"  # Simulamos que siempre responde "guardar clip"
